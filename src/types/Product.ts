export type ProductId = {
  id: number;
};

export type Product = {
  id: number;
  name: string;
  price: string;
  orderId: number;
};

export type ProductCreated = {
  id: number;
  name: string;
  price: string;
};
